//check if a number is armstrong number or not...
#include <stdio.h>
int main(){
   int a;
   scanf("%d",&a);
    while(1){
        int n = a%10;  //54%10 = 4 so n=4
        int c=pow(n,3);
    }
   int re = pow(a,3);

    return 0;
}